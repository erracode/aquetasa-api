{
  "name": "aquetasa-api",
  "version": "1.0.0",
  "description": "Backend API for AQUÉ TA$A - Dollar rates caching service",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create aquetasa-db",
    "db:migrate": "wrangler d1 migrations apply aquetasa-db",
    "db:migrate:local": "wrangler d1 migrations apply aquetasa-db --local",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
